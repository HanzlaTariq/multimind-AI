export const metadata = {
  title: "Sign Up",
  description: "Create a free MultiMind account and start comparing ChatGPT, Claude, Gemini, Groq, and DeepSeek in one workspace.",
};

export default function SignupLayout({ children }) {
  return children;
}