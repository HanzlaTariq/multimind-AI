export const metadata = {
  title: "Log In",
  description: "Log in to your MultiMind account to access ChatGPT, Claude, Gemini, Groq, and DeepSeek in one workspace.",
};

export default function LoginLayout({ children }) {
  return children;
}